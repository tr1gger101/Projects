A very simple python script to download a YouTube video by providing the link.

# Some pre-requistes include :
- Pytube
Note : if pytube isn't installed, install it using the following command : pip install pytube

